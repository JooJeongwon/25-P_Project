spring:
  application:
    name: backend

  datasource:
    # 로컬 실행: localhost로 DB 접속
    url: jdbc:mysql://localhost:3306/hyodream_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        default_batch_fetch_size: 100

  data:
    redis:
      host: localhost # 변경됨: host.docker.internal -> localhost
      port: 6379

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG

naver:
  client-id: Jda4sb7czd6aeWbFLKfn
  client-secret: QofE1CitRJ

ai:
  server:
    # AI 서버 주소 변경
    url: http://localhost:8000 # 변경됨: host.docker.internal -> localhost
  review:
    url: http://localhost:8001 # 변경됨: host.docker.internal -> localhost
crawler:
  url: http://localhost:8002 # 변경됨: host.docker.internal -> localhost